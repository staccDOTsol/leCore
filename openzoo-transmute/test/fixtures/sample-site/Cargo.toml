[package]
name = "sample-site"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.11.2"
zoo-host = { path = "../../../runtime/zoo-host" }

[profile.release]
overflow-checks = false
lto = "fat"
codegen-units = 1
opt-level = 3
