# Private key for the trading wallet. NEVER commit the real value.
# The bot reads this and nothing else; it is never logged or transmitted.
STACCOVERFLOW_KP=0x...

# Optional: override any chain's RPC (comma-free, one per line)
# RPC_137=https://your-polygon-rpc
# RPC_8453=https://your-base-rpc

# Optional: address of an already-deployed OmniArb helper per chain
# ARB_137=0x...
